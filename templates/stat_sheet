<!DOCTYPE html>
<html>
<head>
    <title>Your Stat Sheet</title>
    <style>
        body { background: #201146; color: #fff; font-family: 'Segoe UI', Arial, sans-serif; }
        .container { max-width: 500px; margin: 3rem auto; background: #281b52; border-radius: 16px; padding: 2rem 2rem;}
        .back { margin-bottom: 1em; }
        .statname { color:#ffe066; font-weight:bold;}
        .statval { color:#fff; }
    </style>
</head>
<body>
    <div class="container">
        <a class="back" href="{{ url_for('home') }}">&#8592; Back to Adventure</a>
        <h2>{{ user["Name"] }}'s Stat Sheet</h2>
        <p><span class="statname">Tier:</span> <span class="statval">{{ user["Tier"] }}</span></p>
        <p><span class="statname">HP:</span> <span class="statval">{{ user["HP"] }}</span></p>
        <p><span class="statname">Origin Essence:</span> <span class="statval">{{ user["Origin Essence"] }}</span></p>
        <p><span class="statname">Inventory:</span> <span class="statval">{{ user["Inventory"]|join(", ") }}</span></p>
        <p><span class="statname">Progress:</span> Ch. {{ user["Story"]["chapter"] }}, Scene {{ user["Story"]["scene"] }}</p>
        <div class="history">
            <b>Recent Actions:</b>
            <ul>
            {% for a in user["Story"]["history"][-5:] %}
                <li>{{ a }}</li>
            {% else %}
                <li>No recent actions yet.</li>
            {% endfor %}
            </ul>
        </div>
    </div>
</body>
</html>
